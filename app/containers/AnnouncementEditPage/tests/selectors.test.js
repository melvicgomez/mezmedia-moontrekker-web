// import { selectAnnouncementEditPageDomain } from '../selectors';

describe('selectAnnouncementEditPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

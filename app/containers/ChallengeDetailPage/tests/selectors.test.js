// import { selectChallengeDetailPageDomain } from '../selectors';

describe('selectChallengeDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

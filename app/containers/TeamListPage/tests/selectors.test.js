// import { selectTeamListPageDomain } from '../selectors';

describe('selectTeamListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

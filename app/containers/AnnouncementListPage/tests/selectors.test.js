// import { selectAnnouncementListPageDomain } from '../selectors';

describe('selectAnnouncementListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

// import { selectBadWeatherListPageDomain } from '../selectors';

describe('selectBadWeatherListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

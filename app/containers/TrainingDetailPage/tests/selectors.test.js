// import { selectTrainingDetailPageDomain } from '../selectors';

describe('selectTrainingDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

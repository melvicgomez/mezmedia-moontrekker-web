// import { selectSendNotificationPageDomain } from '../selectors';

describe('selectSendNotificationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

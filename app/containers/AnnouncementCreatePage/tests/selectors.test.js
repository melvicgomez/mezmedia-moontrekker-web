// import { selectAnnouncementCreatePageDomain } from '../selectors';

describe('selectAnnouncementCreatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

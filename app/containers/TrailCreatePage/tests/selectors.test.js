// import { selectTrailCreatePageDomain } from '../selectors';

describe('selectTrailCreatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

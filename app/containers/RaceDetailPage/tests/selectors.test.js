// import { selectRaceDetailPageDomain } from '../selectors';

describe('selectRaceDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

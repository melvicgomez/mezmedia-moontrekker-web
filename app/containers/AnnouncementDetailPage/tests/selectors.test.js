// import { selectAnnouncementDetailPageDomain } from '../selectors';

describe('selectAnnouncementDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

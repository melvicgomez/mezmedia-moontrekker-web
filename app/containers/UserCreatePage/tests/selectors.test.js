// import { selectUserCreatePageDomain } from '../selectors';

describe('selectUserCreatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
